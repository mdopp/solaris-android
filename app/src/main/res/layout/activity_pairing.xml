<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/solaris_bg"
    android:padding="28dp">

    <ImageView
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginBottom="18dp"
        android:src="@drawable/ic_solaris_logo"
        android:contentDescription="@null" />

    <TextView
        android:id="@+id/pairing_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:text="@string/pairing_title"
        android:textColor="@color/solaris_text"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/pairing_status"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="28dp"
        android:textColor="@color/solaris_text_muted"
        android:textSize="15sp" />

    <Button
        android:id="@+id/pairing_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/pairing_action" />

    <Button
        android:id="@+id/pairing_unpair"
        style="@style/Widget.Solaris.Button.Ghost"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/pairing_unpair"
        android:visibility="gone" />
</LinearLayout>
