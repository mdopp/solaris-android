<?xml version="1.0" encoding="utf-8"?>
<!-- One row of the scrollable active-devices collection widget (#28): type icon +
     name + state, Solaris dark look, mirroring item_device_row. RemoteViews-safe
     (LinearLayout/ImageView/TextView only — no plain <View>). The whole row is
     clickable via the collection's PendingIntentTemplate + a per-row fill-in
     intent (tap → PWA). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/av_item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:baselineAligned="false"
    android:paddingTop="9dp"
    android:paddingBottom="9dp">

    <ImageView
        android:id="@+id/av_item_icon"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:src="@drawable/ic_light"
        android:contentDescription="@null" />

    <TextView
        android:id="@+id/av_item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="#FFF2F2F5"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/av_item_state"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:maxLines="1"
        android:textColor="#FFB0BEC5"
        android:textStyle="bold"
        android:textSize="13sp" />
</LinearLayout>
