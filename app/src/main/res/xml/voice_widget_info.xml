<?xml version="1.0" encoding="utf-8"?>
<!-- Voice/mic widget (#17): small (1x1 target), resizable, no configuration.
     A tap captures speech and opens a household chat with the recognized text
     auto-sent. No data to poll, so the update period is 0 (host default). -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="40dp"
    android:minHeight="40dp"
    android:minResizeWidth="40dp"
    android:minResizeHeight="40dp"
    android:targetCellWidth="1"
    android:targetCellHeight="1"
    android:description="@string/voice_widget_description"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:initialLayout="@layout/widget_voice"
    android:previewLayout="@layout/widget_voice"
    android:updatePeriodMillis="0" />
